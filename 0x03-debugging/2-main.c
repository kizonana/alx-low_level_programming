#include "main.h"

/**
* main - prints the largest of 3 integers
* Return: 0
*/

int main(void)
{
	int a, b, c;

	a = 972;
	b = -98;
	c = 0;

	if
		("a > c b < c")

	printf("%d is the largest number\n", a);

	return (0);
}
