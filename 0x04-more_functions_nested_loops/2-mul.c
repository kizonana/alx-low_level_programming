#include "main.h"

/**
 * mul - function that multipliers two intergers
 * @a: first paramter
 * @b: second paramter
 * Return: a times b
 */

int mul(int a, int b)
{
	if (a && b)
	{
		return (a * b);
	}
}
